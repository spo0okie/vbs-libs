v4.0 25.01.21
 * Добавлены библиотеки для инвентаризации
v3.3 25.04.16 
 * Библиотеки слегка подпилены, туда внесены все необходимые инициализации переменных
 + добавлена библотека маппинга шар и принтеров
 * большинство скриптов маппинга переделано на использование библиотек
v3.2 24.04.16
 * Здоровый файл попилен на библиотеки, код почищен от неиспользуемых процедур
 - Убран режим псевдовыполнения: он удобен для дебага, но с другой стороны так 
   увеличивает код, что провоцирует необходимость в дебаге. Все вызовы перехода 
   скрипта в режим ридонли заменены на вызов Halt() с тем же текстом ошибки
 * скрипт поделен на части для упрощения
'v3.1 (15.04.16) 
'-отключена проверка на принадлежность компьютера к группе. Ну ее к черту, и так 
'быстро отрабатывает
'!исправлена переменная сервера лицензий НХ
'v3.0 (17.03.16)
'-Убраны настройки мультисайта (сейчас он норм настраивается через OTW)
'*NX перенастроен на Махачкалу
'-Убран патч MTMDatacard поскольку нужные файлы есть в общем OTW компоненте АЗИМУТ
'v2.9 (27.01.16)
'!исправлена ошибка определения CRLF последовательности в файле (возникала
'в случае если во всем файле отстствовал символ #10 или #13
'!исправлено объявление переменных для патча etc\hosts (переменные были отделены
'знаком = вместо пробела, используемого в hosts файле
'*etc/hosts патч запускается всегда, а не только при использовании мультисайта
'+добавлены патчи NX: переменные окружения NX_ENV_FILE, NX_CUSTOM_DIR (Семенов)
'v2.8 (18.08.2015)
'+Добавлены патчи для настройки мультисайта в ТС
'+Дописана возможность добавления переменной относительно опорной строки
' (если добавляется не в INI файл, а например в BAT
'*START_NXMANAGER использует эту функцию, а не просто добавление строки
'+MTMDatacard patch
'v2.7 (06.07.2015)
'+Дописана мини-библиотека (300строк) для работы с INI-подобными конфиг-файлами
' с различным синтаксисом conf/ini/bat...
'+Патчи для интеграции с альтиумом
'+MTMDatacard patch
'v2.6 (29.06.2015)
'+DirCopy patcher - патч в виде копирования директории через ХСОРУ
'+возможность проверки размера файла блокировки
'+патч устанавливающий русскую локаль, но вообще этот файл уже есть в патче
' Azimut_template, но на всякий случай пусть будет
'v2.5 (10.06.2015)
'+Uninstaller для тимцентра (переустановка делается передачей одновременно
' uninstall install
'v2.4 (10.06.2015)
'+Патчер-установщик шрифтов
'+Патч на установку гостовских шрифтов
'v2.3 (05.06.2015)
'+Снос некорректного визуализатора
'+Установка корректного
'+Процедура проверки применимости патча: проверка наличия обязательного файла,
' опционально поиск ключевых фраз(опционально меняется и логика поиска)
' также проверка отсутствия блокирующего файла или отсутствие в нем ключевых фраз
'+добавлены все патчи для визуализатора
'+включение безопасного режима из коммандной строки (параметр dummy)
'v2.2 (03.06.2015)
'!Включены все системные вызовы
'!исправлена ошибка в парсере текстовых файлов. новые данные в файл
' вставлялись не в новую строку а следом за опорной строкой, если
' сразу за опорной строкой не было переноса
'!куча исправлений, первый боевой пуск на новом движке (установлен в GP).
'v2.1 (01.06.2015)
'+ Патчер текстовых файлов
'v2.0 (01.06.2015)
'-все вызовы внешних программ временно отключны
'*Перенос в VBS движок, тестовый вариант
'!Проверка членства в группе делается проверенным на подключении принтеров способом
'+Добавлен парсер коммандной строки, можно передавать аргументы в произвольном порядке
'v1.1
'+Установка самого ТС
'+Проверка членства в группе (проблемы с совместимостью на разных платформах
'v1.0
'Создает необходимые каталоги и переменные окружения
'Устанавливает необходимую версию джавы
'После успешной отработки нужно перегрузиться, для применения переменных окружения
